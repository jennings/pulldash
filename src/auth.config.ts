// GitHub OAuth App Client ID.
// Set this to enable the "Sign in with GitHub" (device flow) button.
// Create an OAuth App at https://github.com/settings/developers/apps
// (no callback URL needed for device flow), then replace "FIXME" with
// the client ID. Only users who run the server need this — PAT auth
// works without it.
export const GITHUB_CLIENT_ID = "FIXME";
